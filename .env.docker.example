# Docker: copy backend/.env.example → backend/.env and set OPENAI_API_KEY (and SECRET_KEY).
# Compose loads backend/.env into the backend container automatically.
# Optional root .env is only for compose variable substitution, not required for API keys.
